node_modules/
dist/
.env
*.log
*.map
.env*
# Whitelist example/template files — these are committed and act as the
# source of truth for environment-variable contracts.
!.env.example
!.env.*.example
scratch/
scripts/*.xlsx
.vercel

# Generated email artifacts (regenerate via `npm run email:test` / `npm run email:auth`)
tests/email-previews/out/
src/emails/auth-dist/
